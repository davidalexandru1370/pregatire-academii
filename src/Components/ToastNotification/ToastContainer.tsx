import React from "react";
//@ts-ignore
import ReactPortal from "../ReactPortal/ReactPortal.ts";

const ToastContainer = () => {
  return <ReactPortal wrapperId="toastContainer"></ReactPortal>;
};

export default ToastContainer;
